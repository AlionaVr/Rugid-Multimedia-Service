spring:
  profiles:
    active: dev
  application:
    name: multimedia-service
  cloud:
    config:
      uri: http://localhost:8880
      fail-fast: false
rugid:
  storage:
    folder-path: "content"
